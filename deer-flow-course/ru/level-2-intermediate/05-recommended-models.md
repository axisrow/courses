---
title: "Рекомендуемые модели"
weight: 5
bookToc: true
---

# Рекомендуемые модели

## Введение

DeerFlow — **модель-агностик**, то есть работает с любой ИИ-моделью, поддерживающей OpenAI-совместимый API. Но не все модели одинаково хороши для агентных задач. В этом уроке мы разберём, какие модели выбрать.

## Что важно для агентных задач

### 1. Длинное контекстное окно (100K+ токенов)

Агент работает с большим объёмом информации: промпт, навыки, память, история, результаты инструментов. Модели с коротким контекстом быстро «переполняются».

### 2. Способность к рассуждениям (Reasoning)

Агенту нужно планировать, декомпозировать задачи, принимать решения. Модели с поддержкой «thinking» (размышления) делают это лучше.

### 3. Мультимодальность

Поддержка изображений позволяет агенту «видеть» — анализировать графики, скриншоты, фотографии.

### 4. Надёжный вызов инструментов (Tool Use)

Агент постоянно вызывает инструменты. Модель должна формировать правильные вызовы стабильно.

## Рекомендуемые модели

### Топ-выбор

| Модель | Провайдер | Контекст | Reasoning | Vision | Tool Use |
|--------|-----------|----------|-----------|--------|----------|
| GPT-4o | OpenAI | 128K | ✅ | ✅ | ✅✅ |
| Claude 3.5 Sonnet | Anthropic | 200K | ✅ | ✅ | ✅✅ |
| DeepSeek V3 | DeepSeek | 128K | ✅ | ✅ | ✅ |

### Бюджетные варианты

| Модель | Провайдер | Контекст | Цена |
|--------|-----------|----------|------|
| GPT-4o-mini | OpenAI | 128K | Низкая |
| Claude 3 Haiku | Anthropic | 200K | Низкая |
| DeepSeek V3 | DeepSeek | 128K | Очень низкая |

### Модели с поддержкой Thinking

Некоторые модели поддерживают режим «thinking» — расширенное рассуждение:

```yaml
models:
  - name: deepseek-v3
    supports_thinking: true
    when_thinking_enabled:
      extra_body:
        thinking:
          type: enabled
```

## Настройка моделей в config.yaml

### OpenAI

```yaml
models:
  - name: gpt-4o
    display_name: GPT-4o
    use: langchain_openai:ChatOpenAI
    model: gpt-4o
    api_key: $OPENAI_API_KEY
    max_tokens: 4096
    temperature: 0.7
    supports_vision: true
```

### Anthropic (Claude)

```yaml
models:
  - name: claude-sonnet
    display_name: Claude 3.5 Sonnet
    use: langchain_anthropic:ChatAnthropic
    model: claude-3-5-sonnet-20241022
    api_key: $ANTHROPIC_API_KEY
    max_tokens: 4096
    supports_vision: true
```

### DeepSeek

```yaml
models:
  - name: deepseek-v3
    display_name: DeepSeek V3
    use: langchain_deepseek:ChatDeepSeek
    model: deepseek-chat
    api_key: $DEEPSEEK_API_KEY
    supports_thinking: true
```

### Любой OpenAI-совместимый провайдер

```yaml
models:
  - name: local-model
    display_name: Local LLM
    use: langchain_openai:ChatOpenAI
    model: my-model
    base_url: http://localhost:8080/v1
    api_key: not-needed
```

Это позволяет подключить локальные модели (Ollama, vLLM и т.д.).

## Выбор модели для разных задач

| Задача | Рекомендация |
|--------|-------------|
| Исследования | GPT-4o, Claude Sonnet |
| Генерация кода | GPT-4o, DeepSeek V3 |
| Анализ изображений | GPT-4o (supports_vision) |
| Простые задачи | GPT-4o-mini, DeepSeek V3 |
| Длинные разговоры | Claude Sonnet (200K контекст) |

## Переключение моделей

Модель можно сменить:
1. В **интерфейсе** — через выпадающий список
2. В **конфигурации** — параметр `model_name`
3. Через **API** — при создании запроса

## Итоги

- DeerFlow работает с любой OpenAI-совместимой моделью
- Для агентных задач важны: длинный контекст, reasoning, tool use
- GPT-4o и Claude Sonnet — лучший выбор для большинства задач
- DeepSeek V3 — отличный бюджетный вариант
- Можно подключить локальные модели через OpenAI-совместимый API
- Модель можно менять на лету

🎉 Вы завершили второй уровень! Переходите к продвинутому уровню для глубокого погружения в архитектуру DeerFlow.
