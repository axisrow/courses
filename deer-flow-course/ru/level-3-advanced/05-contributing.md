---
title: "Контрибьюция в DeerFlow"
weight: 5
bookToc: true
---

# Контрибьюция в DeerFlow

## Введение

DeerFlow — проект с открытым кодом, и вклад сообщества приветствуется! В этом уроке мы разберём, как настроить среду разработки, внести изменения и отправить Pull Request.

## Настройка среды разработки

### Через Docker (рекомендуется)

```bash
# 1. Форкните репозиторий на GitHub
# 2. Клонируйте ваш форк
git clone https://github.com/YOUR-USERNAME/deer-flow.git
cd deer-flow

# 3. Настройте конфигурацию
cp config.example.yaml config.yaml
cp extensions_config.example.json extensions_config.json

# 4. Установите ключи
export OPENAI_API_KEY="your-key"

# 5. Инициализация и запуск
make docker-init
make docker-start
```

Все сервисы запустятся с hot-reload — изменения в коде применяются автоматически.

### Локальная разработка

```bash
# Проверка зависимостей
make check   # Node.js 22+, pnpm, uv, nginx

# Установка зависимостей
make install

# Запуск
make dev
```

## Структура кодовой базы

### Backend

```
backend/
├── src/
│   ├── agents/        # Система агентов и middleware
│   ├── gateway/       # REST API (FastAPI)
│   ├── sandbox/       # Система песочниц
│   ├── subagents/     # Суб-агенты
│   ├── tools/         # Встроенные инструменты
│   ├── mcp/           # MCP-интеграция
│   ├── models/        # Фабрика моделей
│   ├── skills/        # Система навыков
│   ├── config/        # Конфигурация
│   └── community/     # Community-инструменты
├── tests/             # Тесты
└── docs/              # Документация
```

### Frontend

```
frontend/              # Next.js приложение
```

## Стиль кода

### Python (Backend)

- Линтер и форматтер: **ruff**
- Длина строки: **240 символов**
- Python **3.12+** с type hints
- Двойные кавычки, пробельные отступы

```bash
# Проверка
cd backend
make lint

# Автоформатирование
make format
```

### Запуск тестов

```bash
cd backend
pytest tests/
```

## Процесс контрибьюции

### 1. Создайте Issue

Перед началом работы создайте Issue на GitHub с описанием:
- Что вы хотите изменить/добавить
- Зачем это нужно
- Примерный план реализации

### 2. Создайте ветку

```bash
git checkout -b feature/my-feature
# или
git checkout -b fix/my-bugfix
```

### 3. Внесите изменения

- Пишите тесты для нового кода
- Следуйте стилю кода проекта
- **Обязательно обновите документацию** (README.md, CLAUDE.md)

### 4. Проверьте

```bash
# Линтинг
cd backend && make lint

# Тесты
pytest tests/

# Убедитесь, что приложение запускается
make dev
```

### 5. Коммит и Push

```bash
git add .
git commit -m "feat: add my new feature"
git push origin feature/my-feature
```

### 6. Создайте Pull Request

На GitHub нажмите «New Pull Request» и опишите:
- Что сделано
- Как тестировать
- Связанные Issues

## Что можно контрибьютить

### Навыки (Skills)

Самый простой способ внести вклад — создать новый навык:

```
skills/public/
└── my-new-skill/
    └── SKILL.md
```

### Community-инструменты

Новые интеграции в `backend/src/community/`:

```
src/community/
└── my_tool/
    ├── __init__.py
    └── tools.py
```

### Улучшения документации

- Исправление ошибок
- Перевод на другие языки
- Добавление примеров

### Баг-фиксы

- Проверьте Issues на GitHub
- Ищите метку `good first issue` для начала

## Важные правила

1. **Всегда обновляйте документацию** после изменений кода
2. **Пишите тесты** для нового функционала
3. **Следуйте стилю** существующего кода
4. **Маленькие PR** лучше больших — легче ревьюить
5. **MIT лицензия** — ваш код будет под MIT

## Итоги

- DeerFlow приветствует контрибьюции от сообщества
- Docker — рекомендуемая среда разработки с hot-reload
- Стиль кода: ruff, Python 3.12+, type hints
- Обновляйте документацию с каждым изменением
- Начните с Issues, создайте ветку, сделайте PR
- Простые способы начать: навыки, документация, баг-фиксы

🎉 Поздравляем! Вы завершили весь курс по DeerFlow. Теперь вы знаете, как использовать, настраивать и расширять эту мощную платформу суперагентов!
