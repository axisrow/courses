---
title: "Установка DeerFlow"
weight: 2
bookToc: true
---

# Установка DeerFlow

## Введение

В этом уроке мы шаг за шагом установим DeerFlow на ваш компьютер. Есть два способа: через **Docker** (рекомендуется) и вручную. Docker — это программа, которая создаёт изолированные контейнеры для приложений, как «коробки» с полным набором всего необходимого.

## Что вам понадобится

- Компьютер с Windows, macOS или Linux
- Подключение к интернету
- API-ключ от одного из провайдеров ИИ (OpenAI, Anthropic, DeepSeek и т.д.)

> **API-ключ** — это уникальный пароль, который позволяет вашему приложению общаться с сервисом ИИ. Его можно получить на сайте провайдера (например, platform.openai.com).

## Способ 1: Установка через Docker (рекомендуется)

### Шаг 1: Установите Docker

1. Перейдите на [docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop)
2. Скачайте Docker Desktop для вашей ОС
3. Установите и запустите Docker Desktop

### Шаг 2: Установите pnpm

**pnpm** — это менеджер пакетов для JavaScript (аналог npm, но быстрее).

```bash
# Установка через npm (если есть Node.js)
npm install -g pnpm

# Или через скрипт
curl -fsSL https://get.pnpm.io/install.sh | sh -
```

### Шаг 3: Клонируйте репозиторий

> **Клонировать репозиторий** — это скачать весь код проекта из GitHub на ваш компьютер.

```bash
git clone https://github.com/bytedance/deer-flow.git
cd deer-flow
```

### Шаг 4: Создайте файлы конфигурации

```bash
make config
```

Эта команда создаст файлы `config.yaml` и `.env` из шаблонов.

### Шаг 5: Настройте API-ключи

Откройте файл `.env` в текстовом редакторе и добавьте ваш ключ:

```bash
OPENAI_API_KEY=sk-ваш-ключ-здесь
```

Или, если вы используете Anthropic (Claude):

```bash
ANTHROPIC_API_KEY=ваш-ключ-здесь
```

### Шаг 6: Запустите DeerFlow

```bash
make docker-init    # Только первый раз — подготовка образов
make docker-start   # Запуск всех сервисов
```

### Шаг 7: Откройте в браузере

Перейдите по адресу: **http://localhost:2026**

🎉 Готово! DeerFlow работает.

## Способ 2: Локальная установка

Этот способ требует больше компонентов на вашем компьютере.

### Необходимые программы

- **Node.js 22+** — среда для запуска JavaScript ([nodejs.org](https://nodejs.org))
- **pnpm** — менеджер пакетов
- **Python 3.12+** — язык программирования ([python.org](https://python.org))
- **uv** — менеджер пакетов Python (`pip install uv`)
- **nginx** — веб-сервер (обычно есть в системе)

### Шаг 1: Проверьте зависимости

```bash
make check
```

Эта команда покажет, какие программы установлены, а какие нужно доустановить.

### Шаг 2: Клонируйте и настройте

```bash
git clone https://github.com/bytedance/deer-flow.git
cd deer-flow
make config
```

### Шаг 3: Настройте API-ключи

Отредактируйте `.env`:

```bash
OPENAI_API_KEY=sk-ваш-ключ-здесь
```

### Шаг 4: Запустите

```bash
make dev
```

Откройте **http://localhost:2026** в браузере.

## Управление сервисами

| Команда | Описание |
|---------|----------|
| `make docker-start` | Запустить все сервисы (Docker) |
| `make docker-stop` | Остановить все сервисы |
| `make docker-restart` | Перезапустить сервисы |
| `make docker-logs` | Показать логи (журнал работы) |
| `make dev` | Запустить локально |
| `make stop` | Остановить локальные сервисы |

## Возможные проблемы

### «Port 2026 is already in use»
Порт занят другой программой. Остановите её или измените порт.

### «Docker daemon is not running»
Запустите Docker Desktop.

### «API key is invalid»
Проверьте правильность ключа в файле `.env`.

## Итоги

- DeerFlow можно установить через Docker (проще) или локально
- Для работы нужен API-ключ от провайдера ИИ
- Веб-интерфейс доступен по адресу http://localhost:2026
- Для управления используются команды `make`

В следующем уроке мы сделаем первый запрос к DeerFlow.
