---
title: "Cmd+K: редактирование кода с AI"
weight: 1
bookToc: true
---

# Cmd+K: редактирование кода с AI

## Что такое Cmd+K?

`Cmd+K` (или `Ctrl+K` на Windows/Linux) открывает **встроенную строку ввода** прямо в коде. Вы описываете, что нужно сделать, и AI редактирует код на месте.

## Как использовать

### Редактирование существующего кода
1. **Выделите** код, который хотите изменить
2. Нажмите `Cmd+K` / `Ctrl+K`
3. **Опишите задачу** (например, «добавь обработку ошибок»)
4. Нажмите `Enter`
5. **Проверьте** изменения — примите или отклоните

### Генерация нового кода
1. Поставьте курсор в нужное место
2. Нажмите `Cmd+K` / `Ctrl+K` (ничего не выделяя)
3. Опишите, что нужно (например, «создай компонент формы входа»)

## Хорошие инструкции

Будьте конкретны:

| ❌ Размыто | ✅ Конкретно |
|-----------|-------------|
| «исправь» | «обработай случай, когда список пуст» |
| «сделай лучше» | «добавь валидацию и сообщения об ошибках» |
| «измени стиль» | «сделай кнопку синей с закруглёнными углами» |

## Просмотр изменений

После изменений вы увидите **diff**:
- 🟢 Зелёные строки = добавленный код
- 🔴 Красные строки = удалённый код

Можно: **принять**, **отклонить** или **изменить запрос**.

## Примеры

- Выделить функцию → Cmd+K → «добавь try/catch с понятными сообщениями об ошибках»
- Выделить код → Cmd+K → «конвертируй из JavaScript в TypeScript»
- Выделить код → Cmd+K → «добавь комментарии к каждому шагу»

## Итоги

- `Cmd+K` / `Ctrl+K` = редактирование кода через AI
- Выделите код для редактирования или поставьте курсор для генерации
- Будьте конкретны в инструкциях
- Всегда проверяйте diff перед принятием
