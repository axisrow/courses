# Урок 3. Установка OpenClaw

## Зачем этот урок?

Node.js установлен — отлично! Теперь пора установить сам OpenClaw. Это делается **одной командой** в терминале. Но сначала давайте разберёмся, что такое терминал и как его открыть.

---

## Что такое терминал?

> **Терминал** (он же «командная строка», «консоль») — это программа, в которой вы вводите текстовые команды вместо того, чтобы нажимать кнопки мышкой.

Это как SMS для компьютера: вы пишете ему, что нужно сделать, — он выполняет.

Не бойтесь терминала! Мы будем использовать только простые команды, которые вы просто скопируете и вставите.

---

## Как открыть терминал

### 🍎 macOS (Mac)

**Способ 1 — через Spotlight:**
1. Нажмите **Cmd + Пробел** (одновременно)
2. Напишите **Terminal**
3. Нажмите Enter

**Способ 2 — через Finder:**
1. Откройте **Finder** → **Программы** → **Утилиты** → **Терминал**

Откроется окно с чёрным или белым фоном и мигающим курсором. Это и есть терминал!

---

### 🪟 Windows

**Если вы установили WSL2 (рекомендуемый способ):**
1. Нажмите кнопку **Пуск**
2. Напишите **Ubuntu** (или **WSL**)
3. Откройте появившееся приложение

**Если без WSL2:**
1. Нажмите кнопку **Пуск**
2. Напишите **PowerShell** или **Командная строка**
3. Откройте появившееся приложение

---

### 🐧 Linux

Обычно терминал открывается сочетанием клавиш **Ctrl + Alt + T**.

Или найдите «Terminal» в меню приложений.

---

## Устанавливаем OpenClaw

Теперь самое интересное! Скопируйте и вставьте в терминал одну из этих команд:

### Рекомендуемый способ (скрипт установки)

**macOS / Linux:**
```bash
curl -fsSL https://openclaw.ai/install.sh | bash
```

**Windows (PowerShell, без WSL):**
```powershell
iwr -useb https://openclaw.ai/install.ps1 | iex
```

> Этот скрипт автоматически скачает и установит OpenClaw. Удобно, если вы делаете это впервые.

### Альтернативный способ (через npm)

Если вы уже знакомы с Node.js, можно установить через менеджер пакетов:

```bash
npm install -g openclaw@latest
```

> **Что значит эта команда:**
> - `npm` — менеджер пакетов (программа для установки программ), он пришёл вместе с Node.js
> - `install` — «установить»
> - `-g` — «глобально» (чтобы OpenClaw был доступен из любой папки)
> - `openclaw@latest` — название программы и «последняя версия»

---

## Как вставить команду в терминал

Это частый вопрос у новичков!

- **macOS:** скопируйте команду (Cmd + C), в терминале нажмите **Cmd + V**
- **Windows (PowerShell):** скопируйте (Ctrl + C), в окне кликните **правой кнопкой мыши** — команда вставится
- **Linux:** скопируйте (Ctrl + C), в терминале нажмите **Ctrl + Shift + V**

После вставки нажмите **Enter**, чтобы выполнить команду.

---

## Что произойдёт после установки

В терминале побегут строчки — это нормально, программа скачивается и устанавливается. Через 1–2 минуты всё будет готово.

---

## Проверяем, что OpenClaw установлен

Введите в терминале:

```bash
openclaw --version
```

Если вы видите номер версии (например, `2025.6.15`) — **поздравляем, OpenClaw установлен!** 🎉

Если появилась ошибка — попробуйте закрыть терминал, открыть заново и повторить команду. Иногда после установки нужно перезапустить терминал, чтобы новые команды стали доступны.

---

## Полезные команды

| Команда | Что делает |
|---|---|
| `openclaw --version` | Показывает версию OpenClaw |
| `openclaw --help` | Показывает список всех команд |
| `openclaw doctor` | Проверяет, всё ли настроено правильно |

---

## Итоги урока

- **Терминал** — это текстовый способ управлять компьютером. Открывается за 5 секунд.
- OpenClaw устанавливается **одной командой**: `curl -fsSL https://openclaw.ai/install.sh | bash`
- После установки проверяем: `openclaw --version`
- Не бойтесь терминала — вы просто копируете и вставляете команды!

**В следующем уроке** мы впервые запустим OpenClaw и подключим AI-модель.
