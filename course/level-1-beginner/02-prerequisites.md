# Урок 2. Что нужно для начала

## Зачем этот урок?

Прежде чем установить OpenClaw, нужно подготовить компьютер. Не волнуйтесь — вам понадобится установить всего одну вещь: **Node.js**. В этом уроке мы объясним, что это такое, и пошагово установим его на вашу систему.

---

## Что такое Node.js?

> **Node.js** — это программа, которая позволяет запускать приложения, написанные на языке JavaScript. OpenClaw написан на JavaScript, поэтому без Node.js он не заработает.

Представьте: чтобы открыть файл Word, вам нужен Microsoft Word. Чтобы запустить OpenClaw — нужен Node.js. Это просто «движок» для запуска программы.

**Важно:** вам нужна версия **Node.js 22 или новее**.

---

## Как проверить, есть ли у вас Node.js

Откройте **терминал** (что это такое — подробно расскажем в следующем уроке) и введите:

```bash
node --version
```

- Если вы увидите что-то вроде `v22.12.0` — отлично, Node.js уже установлен!
- Если появится ошибка «command not found» — значит, нужно установить.

---

## Установка Node.js

Выберите вашу операционную систему и следуйте инструкции.

### 🍎 macOS (Mac)

**Способ 1: Скачать с сайта (самый простой)**

1. Откройте в браузере сайт: [https://nodejs.org](https://nodejs.org)
2. Нажмите на зелёную кнопку с версией **22.x LTS** (LTS значит «стабильная версия для долгого использования»)
3. Скачается файл `.pkg` — откройте его
4. Следуйте инструкциям установщика: «Далее» → «Далее» → «Установить»
5. Готово!

**Способ 2: Через Homebrew (если вы знаете, что это)**

```bash
brew install node@22
```

---

### 🪟 Windows

> **Рекомендация от разработчиков OpenClaw:** на Windows лучше всего использовать **WSL2** — это способ запускать Linux прямо внутри Windows. Но если это звучит сложно, можно начать и без него.

**Простой способ: скачать установщик**

1. Откройте в браузере: [https://nodejs.org](https://nodejs.org)
2. Нажмите на кнопку с версией **22.x LTS**
3. Скачается файл `.msi` — откройте его
4. Следуйте шагам установщика: «Next» → «Next» → «Install»
5. **Важно:** поставьте галочку «Automatically install the necessary tools» (автоматически установить необходимые инструменты), если она появится
6. Готово!

**Продвинутый способ: через WSL2 (рекомендуется)**

WSL2 (Windows Subsystem for Linux) — это встроенная в Windows возможность запускать Linux. OpenClaw работает в нём лучше всего.

1. Откройте **PowerShell от имени администратора** (правой кнопкой по кнопке «Пуск» → «Терминал (Администратор)»)
2. Введите:
   ```powershell
   wsl --install
   ```
3. Перезагрузите компьютер
4. После перезагрузки откроется окно Ubuntu — создайте имя пользователя и пароль
5. Теперь в Ubuntu установите Node.js:
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

---

### 🐧 Linux (Ubuntu / Debian)

Откройте терминал и выполните:

```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs
```

> **`sudo`** — это команда, которая запускает действие от имени администратора. Система попросит ваш пароль — это нормально. Когда вы вводите пароль, символы не отображаются — просто наберите его и нажмите Enter.

---

## Проверяем установку

После установки откройте терминал и введите:

```bash
node --version
```

Вы должны увидеть номер версии, например:

```
v22.12.0
```

Если номер начинается с `v22` или выше — всё отлично! ✅

Также проверим, что установился **npm** (менеджер пакетов — программа для установки других программ):

```bash
npm --version
```

Вы должны увидеть номер, например `10.9.0`.

---

## Что ещё понадобится

- 🌐 **Подключение к интернету** — OpenClaw общается с AI-моделями через интернет
- 🔑 **Подписка на AI-сервис** — Anthropic (Claude) или OpenAI (ChatGPT). Подробнее об этом — в Уроке 4
- ⏱️ **15–20 минут свободного времени** — на установку и настройку

---

## Итоги урока

- Для работы OpenClaw нужен **Node.js версии 22** или новее
- Node.js — это «движок», который запускает OpenClaw
- Установить Node.js можно **за 2 минуты**, скачав его с [nodejs.org](https://nodejs.org)
- На Windows рекомендуется использовать **WSL2** для лучшей совместимости
- После установки проверьте версию командой `node --version`

**В следующем уроке** мы установим сам OpenClaw — одной командой!
