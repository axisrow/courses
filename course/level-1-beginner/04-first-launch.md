# Урок 4. Первый запуск и настройка

## Зачем этот урок?

OpenClaw установлен — теперь нужно его настроить. Вы подключите AI-модель (Claude или GPT), запустите шлюз (Gateway) и проведёте свой первый диалог с ассистентом. Звучит сложно? На самом деле — нет: у OpenClaw есть мастер настройки, который проведёт вас за руку.

---

## Шаг 1. Запускаем мастер настройки

Откройте терминал и введите:

```bash
openclaw onboard --install-daemon
```

> **Что значит эта команда:**
> - `openclaw onboard` — запускает **мастер настройки** (wizard). Он задаст вам вопросы и настроит всё автоматически.
> - `--install-daemon` — также установит OpenClaw как **фоновую службу** (daemon), чтобы он работал постоянно, даже после перезагрузки компьютера.

> **Daemon (демон)** — это программа, которая работает в фоне, как будильник на телефоне: вы его не видите, но он всегда готов сработать.

---

## Шаг 2. Подключаем AI-модель

Мастер спросит, какой AI-сервис вы хотите использовать. Есть два основных варианта:

### Вариант A: Anthropic (Claude) — рекомендуется

**Claude** — это AI-модель от компании Anthropic. Разработчики OpenClaw рекомендуют именно её (особенно модели Claude Pro/Max) за качество и безопасность.

**Как подключить:**
1. Мастер предложит войти через **OAuth** (авторизация через браузер)
2. Откроется страница Anthropic — войдите в свой аккаунт или создайте новый
3. Подтвердите доступ
4. Готово — OpenClaw получит подключение к Claude

> **OAuth** — это способ входа, когда вы разрешаете одной программе использовать ваш аккаунт в другой. Как «Войти через Google» на разных сайтах. Вы не передаёте свой пароль — только разрешаете доступ.

### Вариант B: OpenAI (GPT)

Если вы предпочитаете ChatGPT:
1. Мастер также предложит OAuth-авторизацию
2. Войдите в аккаунт OpenAI
3. Подтвердите доступ

> **Подписка:** для работы вам нужна платная подписка на один из сервисов (Anthropic Pro/Max или ChatGPT Plus/Pro). Бесплатные аккаунты обычно имеют сильные ограничения.

---

## Шаг 3. Настройка Gateway

Мастер также настроит **Gateway** (шлюз) — это «сердце» OpenClaw, которое:
- Принимает ваши сообщения из мессенджеров
- Отправляет их AI-модели
- Возвращает ответы вам

Обычно всё настраивается автоматически. Gateway запускается на порту 18789 (это как «адрес комнаты» внутри вашего компьютера).

---

## Шаг 4. Проверяем, что всё работает

После завершения мастера проверим статус:

```bash
openclaw gateway status
```

Вы должны увидеть сообщение о том, что Gateway запущен и работает. ✅

---

## Шаг 5. Первый диалог!

Теперь откроем **панель управления** (Dashboard) — это веб-интерфейс, где можно общаться с AI прямо в браузере:

```bash
openclaw dashboard
```

Откроется браузер с адресом вроде `http://127.0.0.1:18789/`. Вы увидите чат — напишите что-нибудь!

**Попробуйте:**
- «Привет! Расскажи о себе»
- «Какая сегодня погода?»
- «Напиши короткий стих про кота»

Если вы получили ответ — **всё работает!** 🎉

### Альтернатива: диалог через терминал

Можно поговорить с ассистентом прямо в терминале:

```bash
openclaw agent --message "Привет! Кто ты?"
```

---

## Если что-то пошло не так

Запустите диагностику:

```bash
openclaw doctor
```

Эта команда проверит все настройки и подскажет, что нужно исправить.

---

## Полезные команды

| Команда | Что делает |
|---|---|
| `openclaw onboard` | Запустить мастер настройки |
| `openclaw gateway status` | Проверить, работает ли Gateway |
| `openclaw gateway start` | Запустить Gateway |
| `openclaw gateway stop` | Остановить Gateway |
| `openclaw gateway restart` | Перезапустить Gateway |
| `openclaw dashboard` | Открыть веб-интерфейс |
| `openclaw doctor` | Диагностика проблем |

---

## Итоги урока

- Мастер настройки `openclaw onboard` делает большую часть работы за вас
- Вы подключили **AI-модель** (Claude или GPT) через OAuth
- **Gateway** — это фоновая служба, которая связывает вас с AI
- Первый диалог можно провести через **Dashboard** (веб-интерфейс) или через терминал
- Если что-то не работает — `openclaw doctor` поможет найти проблему

**В следующем уроке** мы подключим мессенджер, чтобы общаться с AI прямо в Telegram или WhatsApp!
