# Урок 6. MCP-серверы — подключение внешних инструментов

## Зачем это нужно

Представьте, что Claude Code — это ваш помощник, который сидит за компьютером. По умолчанию он умеет работать с файлами и запускать команды. Но что, если вам нужно, чтобы он проверил задачу в Jira, прочитал сообщение в Slack или заглянул в базу данных?

Для этого существуют **MCP-серверы** — «переходники», которые подключают Claude Code к внешним сервисам и инструментам.

## Что такое MCP

**MCP** (Model Context Protocol, «протокол контекста для модели») — это стандартный способ подключения AI-инструментов к внешним сервисам. Думайте об этом как об USB-портах для Claude: каждый MCP-сервер — это «устройство», которое вы подключаете и получаете новые возможности.

> 💡 **Протокол** — это набор правил общения. Как язык: если оба говорят на одном языке (протоколе), они понимают друг друга. MCP — это «язык», на котором Claude Code общается с внешними сервисами.

## Что можно подключить

| Сервис | Что это даёт |
|--------|-------------|
| **GitHub** | Создавать задачи, pull request'ы, просматривать код |
| **Jira / Asana** | Читать задачи, обновлять статусы |
| **Slack** | Читать и отправлять сообщения |
| **PostgreSQL** | Делать запросы к базе данных |
| **Notion** | Работать с документами и заметками |
| **Figma** | Получать информацию о дизайн-макетах |
| **Sentry** | Анализировать ошибки в приложении |

И многие другие — список постоянно растёт.

## Типы подключения

Есть три способа подключить MCP-сервер:

### 1. Удалённый HTTP-сервер (рекомендуемый)

Сервис работает в облаке, вы просто указываете его адрес:

```bash
claude mcp add --transport http notion https://mcp.notion.com/mcp
```

### 2. Удалённый SSE-сервер

Похож на HTTP, но использует другую технологию передачи данных:

```bash
claude mcp add --transport sse asana https://mcp.asana.com/sse
```

### 3. Локальный сервер (stdio)

Программа запускается прямо на вашем компьютере:

```bash
claude mcp add --transport stdio airtable -- npx -y airtable-mcp-server
```

> 💡 Для начинающих лучше всего подходят удалённые серверы (HTTP/SSE) — они не требуют установки дополнительных программ.

## Пошаговая инструкция: подключение MCP-сервера

### Шаг 1. Выберите сервис

Решите, какой сервис вам нужен. Для примера подключим Notion.

### Шаг 2. Добавьте сервер

Откройте терминал и выполните:

```bash
claude mcp add --transport http notion https://mcp.notion.com/mcp
```

Разберём команду по частям:
- `claude mcp add` — «добавить MCP-сервер»
- `--transport http` — тип подключения (через интернет)
- `notion` — название, которое вы даёте серверу (можно любое)
- `https://mcp.notion.com/mcp` — адрес сервера

### Шаг 3. Авторизуйтесь (если нужно)

Некоторые серверы требуют ключ доступа:

```bash
claude mcp add --transport http my-api https://api.example.com/mcp \
  --header "Authorization: Bearer ваш-ключ-доступа"
```

> 💡 **Bearer token** (токен доступа) — это «пароль» для API-сервиса. Обычно его можно получить в настройках сервиса.

### Шаг 4. Проверьте подключение

Запустите Claude Code и введите:

```
/mcp
```

Вы увидите список подключённых серверов и их статус.

## Управление серверами

### Посмотреть все подключённые серверы

```bash
claude mcp list
```

### Подробности о конкретном сервере

```bash
claude mcp get notion
```

### Удалить сервер

```bash
claude mcp remove notion
```

## Примеры использования

### Работа с задачами из трекера

```
Вы: Посмотри задачу PROJ-123 в Jira и реализуй то, 
    что в ней описано.

Claude: [читает задачу через MCP, понимает требования, 
        пишет код, создаёт коммит]
```

### Анализ ошибок

```
Вы: Проверь последние ошибки в Sentry и исправь 
    самую частую из них.

Claude: [смотрит Sentry, находит ошибку, 
        исправляет в коде]
```

### Работа с базой данных

```
Вы: Найди в базе PostgreSQL 10 пользователей, 
    которые зарегистрировались вчера.

Claude: [подключается к базе через MCP, 
        выполняет запрос, показывает результат]
```

## Где хранится конфигурация MCP

| Уровень | Файл | Для кого |
|---------|------|----------|
| Личный | `~/.claude.json` | Все ваши проекты |
| Проектный | `.mcp.json` в папке проекта | Вся команда |

## Безопасность

- ⚠️ Не делитесь токенами и ключами доступа
- ⚠️ Используйте проектные MCP-серверы только для сервисов, нужных всей команде
- ⚠️ Личные ключи храните в личной конфигурации (`~/.claude.json`)
- ✅ Claude Code спрашивает разрешение перед использованием инструментов MCP

## Советы

- ✅ Начните с одного сервера, убедитесь, что он работает, потом подключайте следующий
- ✅ Используйте `/mcp` для проверки статуса серверов
- ✅ Для командной работы добавляйте серверы в `.mcp.json` проекта
- ✅ Удалённые (HTTP) серверы проще в настройке, чем локальные

## Итоги урока

- **MCP** — это стандарт подключения Claude Code к внешним сервисам (Jira, Slack, базы данных и т.д.)
- Серверы подключаются командой `claude mcp add`
- Есть три типа: HTTP (рекомендуемый), SSE и локальный (stdio)
- Управление: `claude mcp list`, `claude mcp get`, `claude mcp remove`
- Проверка статуса внутри Claude Code: `/mcp`
- MCP расширяет возможности Claude Code далеко за пределы работы с файлами
