# Урок 2. Установка Claude Code

## Зачем это нужно

Чтобы начать работать с Claude Code, нужно его установить на ваш компьютер. Это занимает 2–3 минуты. В этом уроке мы пошагово разберём установку для каждой операционной системы.

---

## Что понадобится

- Компьютер с **macOS**, **Windows** или **Linux**
- Подключение к интернету
- Подписка **Claude Pro, Max, Teams или Enterprise** (оформляется на [claude.com/pricing](https://claude.com/pricing))

> **Операционная система (ОС)** — основная программа вашего компьютера. Mac — это macOS, большинство ПК — Windows, некоторые используют Linux.

---

## Способ 1: Стандартная установка (рекомендуется)

### macOS и Linux

1. Откройте **Терминал**
   - На Mac: нажмите `Cmd + Пробел`, напишите `Terminal`, нажмите Enter
   - На Linux: нажмите `Ctrl + Alt + T`

2. Скопируйте и вставьте эту команду, затем нажмите Enter:

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

3. Дождитесь завершения — это займёт около минуты.

4. Проверьте, что всё установилось:

```bash
claude --version
```

Если вы видите номер версии — всё готово! 🎉

### Windows

#### Вариант A: Через PowerShell

1. Нажмите кнопку **Пуск**, напишите `PowerShell`, нажмите Enter

2. Скопируйте и вставьте эту команду:

```powershell
irm https://claude.ai/install.ps1 | iex
```

3. Нажмите Enter и дождитесь завершения.

#### Вариант B: Через Командную строку (CMD)

1. Нажмите **Пуск**, напишите `cmd`, нажмите Enter

2. Вставьте команду:

```cmd
curl -fsSL https://claude.ai/install.cmd -o install.cmd && install.cmd && del install.cmd
```

3. Нажмите Enter.

> **PowerShell и CMD** — это два разных терминала в Windows. PowerShell более современный, но оба подойдут.

---

## Способ 2: Через Homebrew (только macOS)

**Homebrew** — это менеджер пакетов для Mac. Если вы уже пользуетесь им, установка ещё проще:

```bash
brew install --cask claude-code
```

> **Менеджер пакетов** — программа, которая устанавливает другие программы одной командой. Как App Store, только в терминале.

Если у вас нет Homebrew — используйте Способ 1, он проще.

---

## Способ 3: Через WinGet (только Windows)

**WinGet** — встроенный менеджер пакетов Windows 10/11. Откройте PowerShell и введите:

```powershell
winget install Anthropic.ClaudeCode
```

---

## Способ 4: Десктоп-приложение (без терминала)

Если вы не хотите работать в терминале, скачайте готовое приложение:

- **macOS:** [Скачать для Mac](https://claude.ai/api/desktop/darwin/universal/dmg/latest/redirect) (работает на Intel и Apple Silicon)
- **Windows:** [Скачать для Windows](https://claude.ai/api/desktop/win32/x64/exe/latest/redirect)

Установка стандартная:
1. Скачайте файл
2. Откройте его
3. Перетащите в «Программы» (Mac) или следуйте мастеру установки (Windows)

---

## Проверяем установку

После установки откройте терминал и введите:

```bash
claude --version
```

Вы должны увидеть что-то вроде:

```
claude-code v1.x.x
```

Если видите ошибку «command not found» — попробуйте закрыть и снова открыть терминал. Если не помогает — повторите установку.

---

## Частые проблемы

| Проблема | Решение |
|---|---|
| `command not found` после установки | Закройте и откройте терминал заново |
| Ошибка прав доступа на Mac/Linux | Добавьте `sudo` перед командой: `sudo curl -fsSL...` |
| Нет `curl` на Windows | Используйте PowerShell (Вариант A) вместо CMD |
| Установка зависает | Проверьте подключение к интернету |

> **sudo** (читается «сýдо») — команда, которая запускает другую команду от имени администратора. Как «Запуск от имени администратора» в Windows.

---

## Итоги урока

✅ Claude Code устанавливается **одной командой** в терминале  
✅ Работает на **macOS, Windows и Linux**  
✅ Есть альтернативные способы: **Homebrew**, **WinGet**, **десктоп-приложение**  
✅ После установки проверяйте командой `claude --version`  

---

**Следующий урок:** [Первый запуск и авторизация →](03-first-launch.md)
