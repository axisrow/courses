---
title: "Пружинные анимации"
weight: 9
bookToc: true
---

# Пружинные анимации

Линейные анимации выглядят механически. Пружинные анимации имитируют физику реального мира.

## Что такое пружина?

В жизни пружина подпрыгивает, когда её отпускают. Функция `spring()` создаёт похожее естественное движение.

## Базовая пружина

```tsx
import { spring, useCurrentFrame, useVideoConfig } from "remotion";

export const BounceIn = () => {
  const frame = useCurrentFrame();
  const { fps } = useVideoConfig();

  const scale = spring({
    frame,
    fps,
    from: 0,
    to: 1,
  });

  return (
    <div style={{ transform: `scale(${scale})` }}>
      🎉
    </div>
  );
};
```

## Настройка пружины

```tsx
const scale = spring({
  frame,
  fps,
  from: 0,
  to: 1,
  config: {
    damping: 10,     // Меньше подпрыгивания (больше = меньше)
    stiffness: 100,  // Скорость движения
    mass: 1,         // Вес объекта
  },
});
```

## Spring vs Interpolate

| Свойство | `interpolate()` | `spring()` |
|---|---|---|
| Тип движения | Линейное | Физическое |
| Ощущение | Механическое | Естественное |
| Отскок | Нет | Да |
| Длительность | Фиксированная | Автоматическая |

## Задержка пружины

```tsx
const scale = spring({
  frame: frame - 30, // Начинается с кадра 30
  fps,
});
```

## Итог

`spring()` создаёт естественные анимации с отскоком. Настройте damping, stiffness и mass для контроля.
