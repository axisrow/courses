---
title: "使用组件"
weight: 7
bookToc: true
---

# 使用组件

## 什么是组件？

组件是用户界面的可复用构建块。一个按钮是组件，导航栏是组件，产品卡片也是组件。

把它们想象成乐高积木——你通过组合组件来构建页面。

## 为什么组件重要

- **可复用** — 做一次，到处用
- **一致性** — 所有页面的按钮风格相同
- **速度** — 不用重复设计同样的东西

## 生成单个组件

你可以让 v0 创建具体的组件：

- "Create a notification badge component"
- "Build a user avatar with dropdown menu"
- "Make a search bar with autocomplete"
- "Design a file upload component with drag and drop"

## 常见组件类型

| 组件 | 用途 |
|------|------|
| Navbar | 页面顶部的导航 |
| Card | 包含相关信息的卡片 |
| Modal | 弹出对话框 |
| Form | 输入表单 |
| Table | 数据表格 |
| Sidebar | 侧边栏导航 |
| Footer | 页面底部 |

## 组合组件构建页面

一步一步构建：

1. "Create a navbar with logo and menu links"
2. "Add a hero section below the navbar"
3. "Add a grid of product cards"
4. "Add a footer with social media links"

每一步在同一个对话中补充上一步。

## 总结

组件是界面的构建块。单独生成它们可以获得更多控制，然后组合成完整页面。下一课——设计系统。
